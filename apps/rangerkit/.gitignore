# Build and run artefacts. Note the repo root ignores `deploy/*`, but that
# pattern is anchored there — this package's deploy/ directory is tracked,
# and must be: the unit template, kiosk list and install helper are what the
# app stages render onto the image.
venv/
__pycache__/
*.pyc
.pytest_cache/
