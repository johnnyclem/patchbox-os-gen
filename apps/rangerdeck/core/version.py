"""Single source of the version string.

Read by the grid footer and the install stage's marker file, so a unit in
the field can be asked what it is running without guessing from a directory
listing.
"""

__version__ = "0.1.0"
APP_NAME = "RangerDeck"
