# Pinned dependency set for the 1.0.0 release image.
#
# The install stage tries this first and falls back to the floors in
# requirements.txt package by package, so a pin that will not build under
# qemu costs you a pin and not a boot.
#
# Regenerate on the reference Pi with:
#   pip freeze | grep -iE 'pygame|mido|rtmidi' > requirements.lock
pygame==2.6.1
mido==1.3.3
python-rtmidi==1.5.8

# Development / CI
pytest==9.1.1
