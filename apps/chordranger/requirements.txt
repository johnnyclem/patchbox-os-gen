# ChordRanger runtime dependencies (floors, not pins — see requirements.lock
# for what the image actually installs).
#
# The engine core needs none of these: theory, chords, styles, the arranger
# and the engine itself are stdlib only, which is what lets CI run the whole
# music model on a box with no audio, no MIDI and no display.
pygame>=2.5          # the panel (SDL2 KMS/DRM kiosk)
mido>=1.3            # MIDI message model
python-rtmidi>=1.5   # mido's ALSA sequencer backend
alsa-midi>=1.0          ; platform_system == "Linux"  # native ALSA seq
