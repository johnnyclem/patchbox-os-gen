#!/bin/bash
# One-shot JACK patchbay paint (no button loop).
set -euo pipefail
export PYTHONPATH="/usr/local/lib/patchbox-inky${PYTHONPATH:+:$PYTHONPATH}"
exec /opt/pimoroni/bin/python -m patchbox_inky patchbay "$@"
