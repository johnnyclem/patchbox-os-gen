# ChordRanger (SDL KMS/DRM kiosk + ALSA MIDI)
#
# Almost all of this is already pulled in by stage3/10-install-rk00pi, which
# is fine — apt is idempotent and the two stages must not depend on each
# other's ordering. A build with ENABLE_RK00PI=0 still needs every line here.
python3-venv
python3-dev
python3-pip
build-essential
libasound2-dev
libsdl2-dev
libsdl2-image-dev
libsdl2-mixer-dev
libsdl2-ttf-dev
libfreetype6-dev
libjpeg-dev
libportmidi-dev
libdrm2
libgbm1
rsync
